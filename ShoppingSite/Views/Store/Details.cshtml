@model ShoppingSite.ViewModels.RandomViewModel
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Item.ItemName</h2>

    <p>
        <img alt="@Model.Item.ItemName"
             src="@Model.Item.ItemUrl" />
    </p>
    <div id="item-details">
        <p>
            <em>Category:</em>
            @Model.Category.CategoryName
        </p>
        <p>
            <em>Producer:</em>
            @Model.Producer.Name
        </p>
        <p>
            <em>Price:</em>
            @String.Format("{0:F}",
    Model.Item.Price)
        </p>
        <p class="button1">
            @Html.ActionLink("Add to cart", "AddToCart",
            "ShoppingCart", new { id = Model.Item.ItemId }, "")
        </p>
        <div class="fb-comments" data-width="230" data-numposts="5"></div>
    </div>



